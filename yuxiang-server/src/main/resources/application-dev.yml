server:
  port: 8081
  servlet:
    context-path: /yuxiang-server/

spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    username: root
    password: root
    url:
mybatis:
  mapper-locations: classpath:mapping

logging:
  file:
    path: D:/log/yuxiang-server
  config: classpath:conf/logback-spring.xml
